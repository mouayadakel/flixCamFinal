# Dependencies
node_modules/
/.pnp
.pnp.js

# Testing
/coverage

# Next.js
/.next/
/out/
/build
dist/

# Misc
.DS_Store
*.pem

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local env files — .env.example must NOT be ignored (keep for CI/docs)
.env
.env.local
.env.development
.env.development.local
.env.test
.env.test.local
.env.production
.env.production.local
.env.staging
.env*.local

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Prisma — do NOT ignore migration SQL files; they must be in repo for db:deploy / migrate deploy
# (Only ignore generated client / build artifacts if any; migrations stay tracked.)

# IDE
.vscode/
.idea/
.history/
*.swp
*.swo
*~

# OS
Thumbs.db

# Logs
logs
*.log

# Temporary files
tmp/
temp/
*.tmp

# Local file storage (uploads)
storage/
uploads/

# Sentry
.sentryclirc

# Playwright
/test-results/
/playwright-report/
/playwright/.cache/

# Lighthouse
/reports/

# Audit artifacts (generated)
docs/audit.zip

# Turbo
.turbo/

# VPS server files (not project code)
.bash_history
.gitconfig
.bash_logout
.bash_profile
.bashrc

# AWStats: password file and analytics data (secrets + PII; never commit)
.awstats-htpasswd
awstats/

# VPS deployment directory (production build + runtime, not source)
public_html/FlixCam_Main/FlixCam_Main/
